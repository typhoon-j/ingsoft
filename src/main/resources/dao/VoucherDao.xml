<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bo.ucb.edu.ingsoft.dao.VoucherDao">
    <select id="findByVoucherId" resultType="bo.ucb.edu.ingsoft.dto.Voucher">
        SELECT
               voucher_id as voucherId,
               first_name as firstName,
               last_name as lastName,
               date_voucher as dateVoucher,
               total as total
        FROM voucher
        WHERE
            voucher_id = 2
    </select>
</mapper>
