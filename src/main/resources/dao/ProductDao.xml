<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bo.ucb.edu.ingsoft.dao.ProductDao">

    <insert id="createProduct" parameterType="bo.ucb.edu.ingsoft.model.Product">
        INSERT INTO product
        VALUES (
            null, #{brand}, #{model}, #{price}, #{description},
            #{stock}, 1, #{txId},#{txHost}, #{txUserId}, #{txDate}
        )
    </insert>
    <insert id="updateProduct" parameterType="bo.ucb.edu.ingsoft.dto.ProductUpdate">
        UPDATE product
        SET price = #{price}, stock = #{stock},
            tx_id = #{txId}, tx_host= #{txHost}, tx_user_id= #{txUserId}, tx_date= #{txDate}
        WHERE product_id = #{productId}
        AND tx_status=1
    </insert>
    <insert id="deleteProduct" parameterType="bo.ucb.edu.ingsoft.dto.ProductDelete">
        UPDATE  product
        SET tx_status = #{txStatus},
            tx_id = #{txId}, tx_host= #{txHost}, tx_user_id= #{txUserId}, tx_date= #{txDate}
        WHERE product_id = #{productId}

    </insert>


</mapper>
