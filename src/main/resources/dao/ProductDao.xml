<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bo.ucb.edu.ingsoft.dao.ProductDao">

    <insert id="createBrand" parameterType="bo.ucb.edu.ingsoft.model.Brand">
        INSERT INTO brand
        VALUES (
            null, #{name}, 1,#{txId},#{txHost}, #{txUserId}, #{txDate}
        )
    </insert>
    <insert id="createTag" parameterType="bo.ucb.edu.ingsoft.model.Tag">
        INSERT INTO tag
        VALUES (
            null, #{category}, 1, #{txId},#{txHost}, #{txUserId}, #{txDate}
        )
    </insert>
    <insert id="createProduct" parameterType="bo.ucb.edu.ingsoft.model.Product">
        INSERT INTO product
        VALUES (
            null, #{brandId}, #{productName}, #{tagId}, #{detail},
            #{model}, #{price}, #{stock}, #{description}, #{storeAvailable},#{deliveryAvailable},
            #{image}, 1, #{txId},#{txHost}, #{txUserId}, #{txDate}
        )
    </insert>

    <update id="updateProduct" parameterType="bo.ucb.edu.ingsoft.dto.ProductUpdate">
        UPDATE product
        SET price = #{price}, stock = #{stock},
            tx_id = #{txId}, tx_host= #{txHost}, tx_user_id= #{txUserId}, tx_date= #{txDate}
        WHERE product_id = #{productId}
        AND tx_status=1
    </update>
    <update id="deleteProduct" parameterType="bo.ucb.edu.ingsoft.dto.ProductDelete">
        UPDATE  product
        SET tx_status = #{txStatus},
            tx_id = #{txId}, tx_host= #{txHost}, tx_user_id= #{txUserId}, tx_date= #{txDate}
        WHERE product_id = #{productId}

    </update>
    <select id="getProductbyId" parameterType="int" resultType="bo.ucb.edu.ingsoft.dto.ProductGetId">
        SELECT brand as brand,
               model as model,
               price as price,
               description as description
        FROM product
        WHERE product_id = #{productId}
    </select>

</mapper>
